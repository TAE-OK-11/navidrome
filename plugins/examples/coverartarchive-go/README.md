# Cover Art Archive Go Plugin

Fetches album cover art from the Cover Art Archive using a MusicBrainz release MBID.

```bash
make coverartarchive-go.ndp
```

The plugin implements the MetadataAgent album image capability and uses Navidrome's HTTP host service.
