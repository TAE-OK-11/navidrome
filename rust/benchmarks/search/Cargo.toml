[package]
name = "navidrome-search-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
navidrome-search = { path = "../../search" }

[dev-dependencies]
criterion = { version = "0.5.1", default-features = false }

[[bench]]
name = "search_hotpaths"
harness = false
